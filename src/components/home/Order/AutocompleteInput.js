import React from 'react'
import { useSelector } from 'react-redux';

const AutocompleteInput = ({value,onChange,ptText,enText}) => {
      const language = useSelector((state) => state.loja.language);
    
  return (
<></>
  )
}

export default AutocompleteInput